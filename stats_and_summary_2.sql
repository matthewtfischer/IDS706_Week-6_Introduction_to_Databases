SELECT 
	MAX(publications),
	MIN(publications),
	AVG(publications),
 	SUM(publications),
 	MAX(citations),
	MIN(citations),
	AVG(citations),
 	SUM(citations),
 	MAX(patents),
	MIN(patents),
	AVG(patents),
 	SUM(patents)
FROM university_rankings